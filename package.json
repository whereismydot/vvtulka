{
  "name": "vvtulka",
  "private": true,
  "version": "1.0.1",
  "license": "Apache-2.0",
  "author": "Gleb Perveev",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:all": "vitest run && tsc --noEmit && vite build && vitest run --coverage && node scripts/run-shelf-life-tz-tests.mjs",
    "test:coverage": "vitest run --coverage",
    "test:shelf-life:tz": "node scripts/run-shelf-life-tz-tests.mjs"
  },
  "dependencies": {
    "decimal.js": "^10.6.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "vitest": "^3.2.4"
  }
}
